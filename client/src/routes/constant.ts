/* ROUTE CONSTANTS
   ========================================================================== */

const ROUTES = {
  home: "/",
  notfound: "*",
  login: "/login",
  detail: "/exams/:id",
  dashboard: "/dashboard", // TODO: remove later
};

export default ROUTES;
