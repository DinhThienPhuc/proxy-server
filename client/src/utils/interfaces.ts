/* GLOBAL INTERFACES
   ========================================================================== */

export interface IAppInfo {
  name: string;
  version: string;
}

export type TAppInfo = IAppInfo;
