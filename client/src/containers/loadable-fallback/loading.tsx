/* LOADABLE FALLBACK COMPONENT: LOADING
   ========================================================================== */

const Loading = () => {
  return <div>Loading...</div>;
};

export default Loading;
