/* TEXT-FIELD COMPONENT
   ========================================================================== */

import Styled from "./text-field.style";

const TextField = () => {
  return <Styled.Container>Text field</Styled.Container>;
};

export default TextField;
